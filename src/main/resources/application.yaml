spring:
  application.name: link-shortener

  datasource:
    url: jdbc:postgresql://localhost:5432/link
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  liquibase:
    enabled: true
    change-log: db/changelog/db.changelog.master.yaml

server:
  port: 8090